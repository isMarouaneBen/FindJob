# PostgreSQL 16 with pgvector pre-installed.
# Uses the official pgvector image so we avoid building from source
# (the multi-stage build was failing on networks with strict TLS / no
# access to github.com).
FROM pgvector/pgvector:pg16
